import { css } from 'lit';

export const inputStyle = css`
  input,
  select,
  textarea {
    background-color: rgba(0, 0, 0, 0);
    padding: 6px 8px;
    border-radius: 16px;
    min-width: 120px;
  }
`;
